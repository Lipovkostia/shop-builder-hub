
# Переименование и удаление категорий в диалоге порядка

## Что будет сделано

В диалоге "Порядок категорий" (скриншот) к каждой строке добавятся:
- Кнопка редактирования названия (иконка карандаша) -- по клику название становится полем ввода
- Кнопка удаления (иконка корзины) -- с подтверждением перед удалением

## Изменения

### 1. `src/hooks/useStoreCategories.ts` -- добавить функции rename и delete

Добавить две новые функции в хук:

- **`renameCategory(id, newName)`** -- обновляет `name` в таблице `categories`
- **`deleteCategory(id)`** -- удаляет категорию из таблицы `categories`. Товары, привязанные к этой категории (`primary_category_id`), получат `null` вместо удалённой категории.

### 2. `src/components/admin/CategoryOrderDialog.tsx` -- UI для rename и delete

**SortableCategoryItem** -- добавить к каждой строке:
- Иконку карандаша (Pencil) -- при клике название заменяется на Input, сохранение по Enter / blur, отмена по Escape
- Иконку корзины (Trash2) -- при клике показывается AlertDialog с подтверждением удаления

**Props диалога** -- добавить:
- `onRename: (id: string, newName: string) => Promise<void>`
- `onDelete: (id: string) => Promise<void>`

При удалении категория убирается из локального списка `orderedCategories` без закрытия диалога.

### 3. Места вызова CategoryOrderDialog -- передать новые props

Найти все места, где используется `CategoryOrderDialog`, и передать туда `onRename` и `onDelete`, вызывая соответствующие функции из `useStoreCategories`.

## Техническая часть

```text
SortableCategoryItem
+-------------------------------------------+
| ⠿  [Название категории]  ✏️  🗑️          |
+-------------------------------------------+
       ^                    ^    ^
       |                    |    |
   drag handle         rename  delete

При клике на ✏️:
+-------------------------------------------+
| ⠿  [___input field___]                    |
+-------------------------------------------+
  Enter = сохранить, Escape = отменить
```

Удаление -- AlertDialog:
- Заголовок: "Удалить категорию?"
- Текст: "Категория будет удалена. Товары останутся без категории."
- Кнопки: "Отмена" / "Удалить"
