

## Удаление дублирующей кнопки категорий справа

### Что будет сделано

Удалю блок с кнопкой категорий (иконка-сетка) с правой стороны панели управления на странице витрины. Останется только кнопка каталога (папка) в левом верхнем углу.

### Технические детали

**Файл:** `src/pages/StoreFront.tsx`

- Удалить блок "Правая часть - категории" (строки 1874-1992) -- весь `div` с `DropdownMenu`, содержащий `LayoutGrid` иконку и выпадающий список категорий
- Убрать неиспользуемый импорт `LayoutGrid` из lucide-react (если больше нигде не используется в файле)
- Состояние `expandedDropdownSections` и связанная логика останутся, если используются в другом месте файла
