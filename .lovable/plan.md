

# Новая вкладка "Витрина" в админ-панели

## Что будет сделано

Добавляется новая вкладка "Витрина" в навигацию админ-панели, расположенная между "Розница" и "Опт". Пока без содержимого — только сама вкладка с заглушкой.

## Изменения

### 1. MobileTabNav.tsx

- Добавить `"showcase"` в тип `ActiveSection`
- Добавить элемент навигации `{ id: "showcase", label: "Витрина", icon: Globe }` между "retail" и "wholesale"

### 2. AdminPanel.tsx

- Добавить `"showcase"` в тип `ActiveSection`
- Добавить условие рендера: при `activeSection === "showcase"` показывать заглушку с заголовком "Витрина" и текстом "Настройки витрины появятся здесь"

Никаких новых файлов, хуков, миграций или маршрутов — только вкладка в навигации.

