

# Мессенджеры на витрине: WhatsApp, Telegram, Max

## Что будет сделано

1. В админке во вкладке "Витрина" — три новых поля для мессенджеров (WhatsApp, Telegram, Max)
2. На витрине слева от номера телефона — иконки мессенджеров (только те, для которых заполнены данные)
3. Клик по иконке открывает соответствующий мессенджер

## Миграция БД

Поля `showcase_whatsapp_phone` и `showcase_telegram_username` уже существуют в таблице `stores`. Нужно добавить только одно поле:

```sql
ALTER TABLE stores ADD COLUMN showcase_max_link text;
```

## Изменения в файлах

### 1. AdminPanel.tsx — вкладка "Витрина"

Добавить три поля ввода после поля телефона:

| Поле | Подсказка (placeholder) | Сохраняется в |
|------|------------------------|---------------|
| WhatsApp | +7 999 123-45-67 | `showcase_whatsapp_phone` |
| Telegram | @username | `showcase_telegram_username` |
| Max | max.me/username | `showcase_max_link` |

- Все поля загружаются вместе с `showcase_phone` при открытии вкладки
- Все сохраняются одной кнопкой "Сохранить"

### 2. ShowcaseContactBar.tsx — иконки мессенджеров

Расширить пропсы компонента:

```text
phone, whatsapp, telegram, maxLink
```

Слева от номера телефона отображаются иконки (только заполненные):
- WhatsApp — открывает `https://wa.me/{очищенный номер}`
- Telegram — открывает `https://t.me/{username без @}`
- Max — открывает указанную ссылку напрямую

Компонент рендерится если заполнено хотя бы одно из полей (телефон или любой мессенджер).

### 3. Новый компонент: MaxIcon.tsx

SVG-иконка для мессенджера Max (аналогично существующим `TelegramIcon` и `WhatsAppIcon`).

### 4. GuestCatalogView.tsx и CustomerDashboard.tsx

Расширить запрос к `stores`, чтобы загружать дополнительные поля (`showcase_whatsapp_phone`, `showcase_telegram_username`, `showcase_max_link`), и передавать их в `ShowcaseContactBar`.

## Порядок элементов на витрине

```text
[WhatsApp] [Telegram] [Max]  📞 +7 999 123-45-67
```

Иконки мессенджеров отображаются слева, затем разделитель, затем номер телефона.
