
# План: Добавление "Фиксированной цены" для товаров

## Проблема
Сейчас если у товара есть себестоимость и наценка, цена рассчитывается автоматически. Пользователь хочет иметь возможность вручную установить цену независимо от себестоимости и наценки.

## Решение
Добавить флаг `is_fixed_price` (boolean) в таблицу `products`. Когда включён — используется значение из поля `price` напрямую, наценка игнорируется.

## Архитектура

### Структура данных

```text
Товар с фиксированной ценой:
┌─────────────┬─────────────┬─────────────┬──────────────────┐
│ Себестоимость │   Наценка   │    Цена     │ is_fixed_price   │
├─────────────┼─────────────┼─────────────┼──────────────────┤
│   100 ₽     │     20%     │   150 ₽     │      true        │
└─────────────┴─────────────┴─────────────┴──────────────────┘
        ↓                           ↓
   (игнорируется)           (используется)
```

### Визуальное решение
Рядом с ячейкой "Цена" добавить иконку замка:
- 🔓 **Разблокировано** (по умолчанию) — цена рассчитывается автоматически на основе себестоимости + наценка
- 🔒 **Заблокировано** — цена фиксирована, себестоимость и наценка не влияют

## Изменения

### 1. Миграция БД — добавить поле is_fixed_price

```sql
ALTER TABLE products 
ADD COLUMN is_fixed_price boolean DEFAULT false;

COMMENT ON COLUMN products.is_fixed_price IS 
  'Если true, поле price используется напрямую, игнорируя buy_price и markup';
```

### 2. types/index.ts — обновить интерфейс Product

```typescript
export interface Product {
  // ... existing fields
  isFixedPrice?: boolean;  // Фиксированная цена (игнорировать наценку)
}
```

### 3. MemoizedProductRow.tsx — обновить логику и UI

**Изменить расчёт цены (строки 107-109):**
```typescript
// Если включена фиксированная цена — используем pricePerUnit напрямую
// Иначе рассчитываем на основе себестоимости и наценки
const salePrice = product.isFixedPrice 
  ? product.pricePerUnit
  : (product.buyPrice && product.markup
      ? calculateSalePrice(product.buyPrice, product.markup)
      : product.pricePerUnit);
```

**Добавить обработчик переключения:**
```typescript
const handleToggleFixedPrice = useCallback(() => {
  onUpdateProduct({ ...product, isFixedPrice: !product.isFixedPrice });
}, [onUpdateProduct, product]);
```

**Добавить иконку замка рядом с колонкой "Цена":**
```tsx
{visibleColumns.price && (
  <div className="w-20 flex-shrink-0 flex items-center gap-1">
    <Button
      variant="ghost"
      size="icon"
      className="h-5 w-5 flex-shrink-0"
      onClick={handleToggleFixedPrice}
      title={product.isFixedPrice 
        ? "Фиксированная цена (кликните для расчёта по наценке)" 
        : "Цена по наценке (кликните для фиксации)"}
    >
      {product.isFixedPrice 
        ? <Lock className="h-3 w-3 text-amber-500" /> 
        : <Unlock className="h-3 w-3 text-muted-foreground/40" />}
    </Button>
    <InlinePriceCell
      value={product.pricePerUnit}
      onSave={handleUpdatePrice}
      placeholder="0"
    />
  </div>
)}
```

### 4. useStoreProducts.ts — маппинг полей

При загрузке товаров маппить `is_fixed_price` → `isFixedPrice`.
При сохранении маппить обратно.

### 5. ProductsSection.tsx — маппинг в transformProducts

Добавить преобразование поля `is_fixed_price` в объект Product.

### 6. priceListImport.ts — поддержка в импорте

При импорте цены из Excel, если цена указана явно — автоматически устанавливать `is_fixed_price: true`.

## UX поведение

| Действие | Результат |
|----------|-----------|
| Клик на 🔓 | Переключается в 🔒, текущая расчётная цена фиксируется |
| Клик на 🔒 | Переключается в 🔓, цена пересчитывается по наценке |
| Редактирование цены при 🔓 | Автоматически включается 🔒 |
| Изменение себестоимости/наценки при 🔒 | Цена не меняется |
| Изменение себестоимости/наценки при 🔓 | Цена пересчитывается |

## Альтернативный вариант (без миграции)

Если хотите избежать миграции БД, можно определить "фиксированную цену" по условию:
- Если `markup_value = null` или `markup_value = 0` и `buy_price = null`, считать цену фиксированной

Но это менее явный подход и может привести к путанице.

## Файлы для изменения
- Миграция БД (новое поле `is_fixed_price`)
- `src/components/admin/types/index.ts`
- `src/components/admin/MemoizedProductRow.tsx`
- `src/components/admin/ProductsSection.tsx`
- `src/hooks/useStoreProducts.ts`
- `src/lib/priceListImport.ts`
